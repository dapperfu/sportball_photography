# development/

Scratch scripts from before the `sportball` package existed. Detectors, phase-1 analyzers, one-off YOLO wrappers, sidecar experiments. Some run, some don't, none of them are the supported path.

Use `sportball` / `sb` from the repo root instead.
